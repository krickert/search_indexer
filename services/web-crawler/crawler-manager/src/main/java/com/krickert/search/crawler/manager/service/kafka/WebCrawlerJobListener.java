package com.krickert.search.crawler.manager.service.kafka;

/**
 * A listener from kafka to take in a protocol bugger web crawler job
 * and start it in job runr.  It is assumed that the job has already been configured
 */
public class WebCrawlerJobListener {
}
